# word-cloud


### The server
An express server.

To run the server:

```
cd server
npm i
npm start
```

### The client
Built with Create React App.

To run the client:
```
cd client
npm i
npm start
```
